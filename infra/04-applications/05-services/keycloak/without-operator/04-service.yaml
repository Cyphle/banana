---
apiVersion: v1
kind: Service
metadata:
  name: keycloak-service
  namespace: banana
  labels:
    app: keycloak
spec:
  type: ClusterIP
  selector:
    app: keycloak
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP